import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {},
  palette: {
    primary: {
      main: '#FF0101',
    },
    secondary: {
      main: '#283845',
    },
  },
});
export default theme;
