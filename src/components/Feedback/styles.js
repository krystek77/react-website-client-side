import { makeStyles } from '@mui/styles';

export default makeStyles((theme) => ({
  feedBack: {
    marginBottom: 48,
    color: theme.palette.secondary.dark,
    textAlign: 'center',
    flexGrow: 1,
  },
}));
